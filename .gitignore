# Dossiers generes (build Alire/GNAT)
/obj/
/bin/
/alire/
/config/
/share/

# Fichiers sensibles (secrets, cles, identifiants)
.env
.env.*
*.pem
*.key
*.token
id_rsa*
*credentials*
*.pfx
*.p12

# Sorties generees par les modes map et track (ne pas versionner)
/out/
#  NB : la copie publiee du visualiseur vit dans docs/index.html (versionnee).

# Artefact du serveur de langage Ada
/.als-alire
